server:
  port: 7003
logging:
  level:
    root: INFO
    lb:
      microservice: DEBUG
#for test
spring.jpa.hibernate.ddl-auto: update
---
spring:
  config:
    activate:
      on-profile:
      - docker
server:
    port: 8080
